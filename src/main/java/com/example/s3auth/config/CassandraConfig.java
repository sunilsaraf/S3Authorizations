package com.example.s3auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CassandraConfig {
    // Using Spring Boot auto-configuration for Cassandra via application.yml
    // Additional advanced settings (custom sessions, pooling) can be added here if required
}